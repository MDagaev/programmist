Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.4
Creation-Date: 2019-12-08T12:25:47+08:00

====== 16.Функции-стрелки, основы ======
Создан Воскресенье 08 декабря 2019

«функции-стрелки» или «стрелочные функции» (arrow functions), т.к. выглядит следующим образом:

**let func = (arg1, arg2, ...argN) => expression**
Такая запись равна
**let func = function(arg1, arg2, ...argN) {**
**  return expression;**
**};**
если нет аргументов
**let func = () => expression**

**let welcome = (age < 18) ?**
**  () => alert('Привет') :**
**  () => alert("Здравствуйте!");**

**welcome(); // теперь всё в порядке**

Без фигурных скобок: (...args) => expression – правая сторона выражение: функция выполняет его и возвращает результат.
С фигурными скобками: (...args) => { body } – скобки позволяют нам писать многострочные инструкции внутри функции, но при этом необходимо указывать директиву return, чтобы вернуть какое-либо значение
